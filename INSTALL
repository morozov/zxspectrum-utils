Dependencies
============

All utilities need working C or C++ compiler. The following utilities need
another dependencies, else will be not compiled:

dithvide: libx and libjpeg
divideogen: libpng
xzxs: libx

Installing
==========

1. autoreconf --install (only for SVN code)
2. ./configure
3. make
4. make install

